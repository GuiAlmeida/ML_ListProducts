const setProducts = (state, products) => {
   state.products = products;
 };
const setProductsHeading = (state, heading) => {
   state.heading = heading;
 };
 
 export default { setProducts, setProductsHeading };
 
 
 
 
 